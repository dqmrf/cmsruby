%nav
  =link_to "МішаPress", root_path, {class: "navbar-brand"}
  #main-nav
    %ul.nav.navbar-nav
      -@menus.find {|menu| menu.name == "Main Menu"}.menu_items.each do |item|
        %li
          =link_to item.title, item.url